import { HTMLAttributeAnchorTarget } from 'react';
import { icons } from '../Icon';
import type * as Stitches from '@stitches/react';
declare type ButtonComponentProps = {
    tag?: 'button' | 'a';
    disabled?: boolean;
    text: string;
    onContrast?: boolean;
    border?: boolean;
    href?: string;
    target?: HTMLAttributeAnchorTarget | undefined;
    css?: Stitches.CSS;
    type?: 'button' | 'submit' | 'reset';
    onClick?: () => void;
    icon?: keyof typeof icons;
    iconSize?: number;
    iconEnd?: boolean;
    link?: boolean;
};
export default function Button({ disabled, tag, onContrast, border, text, href, target, type, css, onClick, icon, link, iconSize, iconEnd, ...props }: ButtonComponentProps): JSX.Element;
export {};
